
Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если НЕ ЗначениеЗаполнено(ТипПакетаДанных) Тогда
		ТипПакетаДанных = Перечисления.ТипыПакетовДанныхСУЗ.XML_V8;
	КонецЕсли;

КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	
	НомерРедакции = ?(НомерРедакции = 0, 1, НомерРедакции);
	
КонецПроцедуры


