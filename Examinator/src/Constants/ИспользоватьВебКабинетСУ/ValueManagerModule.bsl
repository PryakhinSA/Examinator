//--# КОНСТРУКТОР КУРСОВ УДАЛИТЬ #--//

Процедура ПриЗаписи(Отказ)
	УстановитьПривилегированныйРежим(Истина);
	Если ПолучитьФункциональнуюОпцию("ПолныйИнтерфейсСУО") Тогда
		Константы.ИспользоватьПинКодыСУО.Установить(ЭтотОбъект.Значение);
	Иначе
		Константы.ИспользоватьПинКодыСУО.Установить(Ложь);
	КонецЕсли;
КонецПроцедуры

//----------------------------------//