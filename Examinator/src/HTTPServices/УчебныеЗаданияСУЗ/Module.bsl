
Функция ПроверитьЗаданиеPOST(Запрос)
	
	ДанныеДляОбработкиXML = Запрос.ПолучитьТелоКакСтроку();
	
	УчебнаяКонфигурацияСУЗ.ВыполнитьПодключение(ДанныеДляОбработкиXML);
	
	Ответ = Новый HTTPСервисОтвет(200);
	Ответ.Заголовки.Вставить("Content-type", "text/html; charset=utf-8");
	
	Ответ.УстановитьТелоИзСтроки(ДанныеДляОбработкиXML);
	
	Возврат Ответ;
	
КонецФункции