
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЗаписьОбъект = РеквизитФормыВЗначение("Запись");
	
	Если ТипЗнч(ЗаписьОбъект.Данные) = Тип("ХранилищеЗначения") Тогда
		Данные = ЗаписьОбъект.Данные.Получить();
	КонецЕсли;
	
КонецПроцедуры
